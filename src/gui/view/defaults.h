#pragma once

namespace UnTech {
namespace View {

const static int DIALOG_TEXT_WIDTH = 300;

const static int SIDEBAR_WIDTH = 350;

// size of comma and cross in the aabb structs
const static int AABB_COMMA_WIDTH = 12;

// size of a small text box for entering digits
const static int SMALL_TEXT_DIGITS_WIDTH = 32;
}
}
